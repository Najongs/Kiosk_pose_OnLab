{
  "name": "onlab-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OnLab — 브라우저(WASM)에서 포즈추정하는 스트레칭/유연성 테스트 웹앱",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "test": "vitest run"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
